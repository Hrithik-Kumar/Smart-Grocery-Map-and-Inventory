import 'package:flutter/material.dart';

class CompanyCreateItemForm extends StatelessWidget {
  const CompanyCreateItemForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
